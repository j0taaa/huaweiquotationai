services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: huaweiquotationai-web
    ports:
      - "3000:3000"
    environment:
      - DATABASE_PATH=/app/data/app.db
    volumes:
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data:
